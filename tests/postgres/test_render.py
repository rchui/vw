"""Tests for PostgreSQL-specific rendering in vw/postgres.

This file tests the rendering of PostgreSQL-specific states and expressions.
ANSI SQL rendering tests are in tests/core/test_render.py.
"""

# TODO: Add tests for PostgreSQL-specific rendering:
# - Now (NOW() function)
# - Interval (INTERVAL 'amount unit')
# - DateTrunc (DATE_TRUNC('unit', expr))
# - ILike, NotILike (case-insensitive pattern matching)
# - Rollup, Cube, GroupingSets (grouping extensions)
